# Pilot 1 one-shot preparation result v0.1

## Outcome

The one-shot observed-residual search package is prepared and frozen for final
independent Fable review. Preparation passed 16/16 controls. No observed
residual was calculated or searched, and execution remains locked.

## Locked search

- target: PSR J1744-1134;
- one 30–2,000-day covariance-whitened circular scan;
- 941 fixed frequency cells at one-fifth-independent-bin spacing;
- strict threshold: delta chi-square greater than 23.33426855482562;
- annual mask cells: indices 67, 64, and 61, nearest the frozen 350, 365.25,
  and 380-day targets;
- strongest unmasked cell has candidate authority;
- no robust refit, alternate threshold, alternate grid, or automatic retry.

## Fable corrections implemented

All 433 paired-row and 316 UTC-day deletion checks remain available only as
advisory diagnostics. Their labels cannot create, rescue, demote, veto, or
trigger further analysis. Every outcome carries the clustered-day borderline
tail result and both failed hard-veto cost results. No new randomness or tail
reroll is permitted.

## Integrity outcome

The complete package manifest, exact one-shot plan, implementation, candidate
schema, and eight external prerequisite records verified without mismatch. All
98 automated tests and static checks passed. The runner independently walks
every manifest entry before it can recognize authorization.

The final Fable sign-off and explicit user authorization records are absent,
as required. No one-shot execution-intent sentinel or result record exists.

## Next gate

Give `docs/FABLE_FINAL_PREEXECUTION_REVIEW_PACKET_v0.1.md` and the repository to
Fable for final hash-bound review. A clean Fable PASS permits presentation of
the exact package to the user for a separate execution decision; it does not
execute or authorize the search itself.
