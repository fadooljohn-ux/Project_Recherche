# TPA batch 06

The owner authorized loading and launching the next 25 eligible TPA targets on
2026-09-08. **Complete: 25 NO_TRIGGER results, zero candidates, zero observed
execution failures and zero noise-adequacy flags.** All run01 destinations are
consumed; no background job remains active. Recherche remains private.
The saved ranking excludes all 85 prior TPA targets and checks eligible,
unsearched status and original PAR/TIM hashes against the current master.
Selection leaves 81 eligible targets unselected.

The existing adapter includes the singleton ECORR repair from batch 05.
External data: `../Project Recherche Data/tpa-batch06-20260908`.

## Preparation repair

J1338−6204 exposed a covariance mismatch before calibration or observed searches.
Archive-name ECORR groups included arrivals separated by at least one second;
PINT instead uses one-second TDB buckets anchored at each bucket's first arrival.
The TPA adapter now constructs those buckets independently, excludes singletons
and retains its full covariance equality check against PINT. The qualified solver
and shared MPTA adapter remain unchanged. All 85 previous TPA preparations passed
that same equality check and their results remain preserved.

The failed preparation and three earlier profiles are retained in `pre-repair/`.
The batch is prepared afresh with the repaired adapter. A focused regression
checks unsorted arrivals, the exact one-second boundary, singleton exclusion
and prevents chaining adjacent arrivals into an overlong bucket. All five TPA
tests pass. This repair uses no observed-search result or threshold tuning.

## Calibration and execution

The batch contains 10,484 TOAs across 1,334 epochs. All 25 final preparations
passed full white-covariance equality against PINT, epoch-DM coverage and
full timing-column-rank checks. Six profiles produced before removal of an
unused variable were preserved under `pre-final-source/` and regenerated so
all final receipts bind the identical adapter source. Ruff also passed.

Calibration used 4,096 nulls per target (102,400 total), starting with seed
2026091600. All 25 noiseless 100-day, 1,000-μs development injections recovered
amplitude within 1.8e-12 μs. Observed searches reused every frozen calibration
cache and mask: one circular 30–400-day grid, oversampling five, with a conditional
1% false-alarm allowance across this 25-target batch. The allowance is not
cumulative across all batches. Thresholds span 26.194091–26.541567. No noise
parameter, threshold or scope was changed after freeze.

Median worst-phase amplitudes for conditional 95% on-grid detection span
502.256–2,183.869 μs between targets. These fixed-noise sensitivities are not
planet masses or posterior upper limits; they do not include masked cells,
arbitrary eccentricity or published-noise parameter uncertainty. This later
sample has weaker sensitivity, which is retained rather than tuned away.

The strongest eligible statistic was J1133−6250 at 41.335245 days: **18.664508**,
below its **26.244727** threshold. This is a sub-threshold peak, not a detected
orbital period. Null reduced χ² spans 0.986256–1.107229; no χ² ≥ 2 flag was
raised. That alone does not establish complete model adequacy. NO_TRIGGER does
not rule out planets below sensitivity or outside the tested scope.

Every search passed its independent full-design peak likelihood check and
updated the master and source queue before the next run. Closeout verified
saved grids, masks, thresholds and likelihood agreement without new scans or
fits. All 109 historical observed-result hashes are unchanged. One resolved
preparation failure is retained separately from zero observed execution failures.
Prior-search cross-matching limitations remain as documented in
[batch 01](TPA_BATCH01_2026-09-08.md); no never-searched claim is made.

Individual results: [observed-summary.csv](../results/observed/tpa-batch06-20260908-observed-summary.csv).

## Evidence and queue

Launch source commit: `b2ce367`. The qualified solver, calibration implementation
and shared MPTA adapter are unchanged from `d3ec6a3`.

- Selection SHA-256: `aa7dc82ae2f0aa7f4ad390810d65a59ea2811f4802deb69c9130205b6466f356`.
- Preparation SHA-256: `fb0f014c5cdce41ad26c01a158aab7d12473b731f987932815ecd0b3d9902989`.
- Execution freeze SHA-256: `d9295792b210df248cce4b41f2363f7033a5a1c6254dea66da14ccad56bfa1f7`.

Tracked evidence is under `results/calibration/tpa-batch06-20260908-*` and
`results/observed/tpa-batch06-20260908-*`. Archives under
`../Recherche Recovery/20260907T223404/` have verified member hashes and include
the original preparation failure:

- `tpa-batch06-preparation-20260908.tar`: 404 files, 58,675,200 bytes,
  SHA-256 `46f3f1128fd1f3740b9ad382a5da0c8095884581a51af37f085c665aa5e3d740`.
- `tpa-batch06-observed-20260908.tar`: 483 files, 59,279,360 bytes,
  SHA-256 `ee1008b9e0ef4274279a850a01f312824b1cbcbe69f8a199c285525e3275e604`.

The master now contains 685 targets, 133 observed targets and 134 search/refinement
records. TPA totals are **110 NO_TRIGGER searches, 47,181 TOAs and 5,949 epochs**,
with **81 eligible targets remaining** and 406 exclusions preserved.
NANOGrav 15, PPTA DR3 and EPTA DR2 remain queued in that order.
