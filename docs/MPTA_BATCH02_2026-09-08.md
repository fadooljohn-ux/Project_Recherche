# MPTA batch 02 preparation

Current status: the owner subsequently authorized “Launch”. All four observed
searches completed once on 2026-09-08: **4 NO_TRIGGER, 0 candidates**. The master
was updated after each run. All four `run01` destinations are now consumed.

Owner instruction, 2026-09-08: keep the master workbook updated after each run
and prepare the next batch. Scope is input preparation, target calibration and
an execution freeze. Observed searches await a launch instruction.

The first inventory admitted 14 targets under its model and coverage rules.
Batch 01 consumed ten. Batch 02 takes the remaining four in the original
prospective ranking order, without inspecting their observed periodograms:

| Target | TOAs | Epochs | Span (days) | Earlier comparison inventories |
| --- | ---: | ---: | ---: | --- |
| J2322+2057 | 1,756 | 79 | 1,362.2 | JBO, EPTA |
| J1801-1417 | 3,069 | 96 | 1,558.7 | JBO, EPTA |
| J1744-1134 | 2,957 | 99 | 1,558.7 | JBO, NANOGrav11, EPTA |
| J0030+0451 | 2,880 | 93 | 1,362.2 | JBO, NANOGrav11, EPTA |

These are previously studied pulsars with independent MeerKAT observations.
They are not claimed to be historically unsearched. The master contains no
prior Recherche observed search for any of these four. No binary, scattering,
short-span or deterministic-chromatic exclusion is relaxed to fill ten slots.
J1801-1417 includes the published intrinsic red process already supported by
the fixed-covariance adapter; the other three have no such Table 1 component.

Use the already retained MPTA 4.5-year archive and original noise tables from
[batch 01](MPTA_BATCH01_2026-09-08.md). New data root:
`../Project Recherche Data/mpta-batch02-20260908/`. Keep the same 30–400-day grid,
oversampling five, fixed published covariance, epoch DM projection, 20% retained
power mask and conditioning criterion. Each profile uses 4,096 Gaussian nulls,
with seeds 2026090900–2026090903 and `search_count=4`. The fixed-Gaussian union
bound allocates a 1% false-alarm allowance to this four-target batch. That is a
batch-level allowance, not a cumulative 1% guarantee across both batches;
the union of their two allowances is at most 2% under their stated models.

Preparation constructs timing residuals for the input profile but runs no
observed periodogram. Calibration reads covariance/design and synthetic nulls.
Sensitivity remains conditional circular on-grid 95% detection probability,
not a posterior upper limit or a noise-parameter-marginalized exclusion.

The small adapter changes reuse an earlier inventory while carrying forward
all prior selections, take seeds from the selected batch policy, and report
the actual frozen search count. Historical source snapshots and results remain
unchanged. The supported `mpta-batch run` command updates the existing master
from its saved result or incomplete-attempt evidence. A workbook failure must
be repaired with `tools/recherche master-workbook DATA_ROOT`, without a new scan.

Do not execute the prepared `run` commands until launch is authorized. No new
monitoring is needed for preparation-only work.

## Prepared and verified

All four profiles and their calibration caches are complete: 10,662 TOAs,
367 epochs, 16,384 synthetic null draws, and zero observed searches. All phase
arcs are below 0.022 cycles. The master marks all four targets as prepared and
retains its 15 previously processed targets and 16 existing search/refinement
records unchanged.

| Target | Fixed threshold | Grid cells | Masked cells | Median worst-phase 95% amplitude (microseconds) |
| --- | ---: | ---: | ---: | ---: |
| J2322+2057 | 22.696102 | 212 | 10 | 2.396 |
| J1801-1417 | 22.960805 | 242 | 9 | 1.755 |
| J1744-1134 | 22.960805 | 242 | 9 | 0.401 |
| J0030+0451 | 22.696102 | 212 | 8 | 1.969 |

Sensitivity is the median across eligible cells of the worst-phase amplitude
for 95% detection probability under the fixed model. No observed candidate
status or observed noise-adequacy diagnostic has been computed for these targets.

Selection SHA-256: `527c974834c7300624851cd17d199b06624ebbcf9b566a557859a7459370c425`.
Execution-freeze SHA-256: `053c52d73da09bf55b22e1c51b258063cff11d6bbd05e5f13827db1625bbd740`.
Scientific adapter source commit: `7a90de58d2ac4d4cf40f41add66799868a472840`.
The freeze binds the exact scientific code and calibration implementation.
Later workbook-formatting or documentation changes do not change these bindings.

Five focused checks passed, including a new cumulative selection-exclusion
check. Static checks passed. All four profile/cache/input bindings were checked
without recalibration. All 16 historical result-file hashes and the qualified
source/calibration implementation matched their retained records. Workbook
historical rows, operator notes, filters and frozen identifiers are preserved;
its formulas and the changed views were checked after import/export.

The 64-file preparation archive was verified by file set and content:
`../Recherche Recovery/20260907T223404/mpta-batch02-prepared-20260908.tar`.
SHA-256: `1b0bb4319f4c38794a75c144dcf8f33d0274081247123e2fc1f20c65c1935dde`.
Compact preparation, freeze, selection, workbook-update and backup receipts are
under `results/observed/mpta-batch02-20260908-*`.

Historical launch commands, now completed and consumed. Do not repeat:

```sh
tools/recherche mpta-batch run --data '../Project Recherche Data/mpta-batch02-20260908' --target J2322+2057
tools/recherche mpta-batch run --data '../Project Recherche Data/mpta-batch02-20260908' --target J1801-1417
tools/recherche mpta-batch run --data '../Project Recherche Data/mpta-batch02-20260908' --target J1744-1134
tools/recherche mpta-batch run --data '../Project Recherche Data/mpta-batch02-20260908' --target J0030+0451
```

A workbook failure does not authorize repeating a scientific scan. At preparation
closeout no `run01` directory existed; the launch below has now consumed all four.

## Observed closeout

The four searches completed between 11:06:03 and 11:07:06 UTC on 2026-09-08.
Each evaluated its frozen grid once and retained the independent full-design
likelihood audit. There were no execution failures, replacements or changes
to scientific thresholds, masks or noise assumptions.

| Target | Strongest eligible period (days) | Statistic | Fixed threshold | Result |
| --- | ---: | ---: | ---: | --- |
| J2322+2057 | 147.978643 | 6.580256 | 22.696102 | NO_TRIGGER |
| J1801-1417 | 119.016409 | 10.148213 | 22.960805 | NO_TRIGGER |
| J1744-1134 | 167.662452 | 12.527062 | 22.960805 | NO_TRIGGER |
| J0030+0451 | 94.562452 | 13.714674 | 22.696102 | NO_TRIGGER |

These are subthreshold timing peaks, not detected orbital periods. Null reduced
chi-square ranges from 0.979 to 1.008, below the predeclared >=2 inadequacy flag.
The fixed-noise and on-grid sensitivity limitations above still apply. No
qualifying circular signal in the searched eligible cells does not establish
absence of companions.

Saved periodograms reproduced the selected peaks, masks and threshold decisions.
The stored independent full-design fits agree with post-peak chi-square and
likelihood improvements for all four targets. All 16 earlier result hashes and
the qualified scientific source/calibration implementation remain unchanged.
Verification ran no new scans, fits or calibration trials.

The master has 88 targets, 19 with observed searches, 20 search/refinement records,
zero current candidates, and the same two data gaps. Its historical search rows
and all operator notes are unchanged. Automatic updates completed after each
scientific run; a final spreadsheet-only adjustment retained numeric formats
for appended rows. No scientific command was repeated for that adjustment.

Terminal evidence: `results/observed/mpta-batch02-20260908-closeout.json`,
`mpta-batch02-20260908-summary.csv` and the workbook update receipt in that folder.
The 76-file terminal archive has verified file-set, content and source-after
hash parity: `../Recherche Recovery/20260907T223404/mpta-batch02-terminal-20260908.tar`.
Archive SHA-256: `f207b761c247df35089fe0fbb653271f9aa0cb6ed49a8e2ecba20770edb4299e`.
Both original-search MPTA batches now total 14 pulsars and 36,023 TOAs with no
qualifying candidates. No run or monitor is active. Further work requires a new
dataset or separately defined scope; these completed runs remain preserved.
