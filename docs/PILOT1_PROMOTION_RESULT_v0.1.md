# Initial-v0.1 promotion result

## Outcome

**FAIL — 20 of 22 hard rows pass. Initial v0.1 is not authorized.**

The detector itself cleared the false-positive, recovery, numerical,
reproducibility, and MacBook resource gates. The failed rows are experimental
sensitivity bracketing and reconciliation with the published 100-day
J1744-1134 reference. No averaging or compensating score is permitted.

## Corrective gate

Before adding lower-amplitude injections, create a discrepancy dossier that:

1. verifies the published 0.56-lunar-mass quantity, confidence convention,
   orbital convention, and target/data selection against the page-supported
   source;
2. independently verifies the timing-amplitude-to-projected-mass conversion;
3. tests whether the released Gaussian covariance simulation omits residual
   structure that materially weakens real-data sensitivity, without running a
   blind observed-residual search; and
4. only then freezes a lower-amplitude matrix capable of bracketing 50% and 90%
   recovery.

The existing failed result and all raw case artifacts remain immutable.
